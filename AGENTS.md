# 项目协作规则

## 语言要求

除非用户明确要求英文，否则你必须优先使用简体中文回复。

以下内容必须使用中文：
- 任务计划
- 修改说明
- 操作步骤
- TODO
- 总结报告
- 错误原因分析
- 测试结果说明
- Git 提交建议
- 给用户看的解释

以下内容可以保留英文：
- 代码
- 变量名
- 函数名
- 文件名
- 文件路径
- API 路径
- JSON 字段名
- 命令行命令
- 依赖名
- 第三方库名称
- 报错原文
- Git commit message，如果用户要求英文

## 修改原则

1. 不要为了中文化而修改变量名、函数名、接口字段、文件路径。
2. 面向用户的网页文案优先使用中文。
3. 面向开发者的解释、报告、步骤优先使用中文。
4. 技术词可以中英混用，比如 API、JSON、React、Vite、Node、npm、localStorage。
5. 修改代码后要说明改了哪些文件、为什么改、如何验证。
6. 如果运行了测试或构建，要明确写出命令和结果。

## 回复格式

每次完成任务后，按这个格式回复：

1. 修改了哪些文件
2. 每个文件改了什么
3. 是否影响功能逻辑
4. 验证结果
5. 下一步建议

不要使用大段英文说明。
不要把普通任务说明写成英文 bullet list。